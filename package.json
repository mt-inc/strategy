{
  "name": "@mt-inc/strategy",
  "version": "1.0.16",
  "description": "Strategy for MT Inc. Project",
  "main": "./dist/cjs/index.js",
  "module": "./dist/esm/index.js",
  "type": "commonjs",
  "author": "Maksym Shamko",
  "license": "MIT",
  "files": [
    "/dist"
  ],
  "scripts": {
    "tsc": "rm -rf ./dist && tsc -p tsconfig.json && tsc -p tsconfig-cjs.json",
    "push": "git push origin master",
    "pull": "git pull origin master",
    "commit": "git add . && git commit -m 'working'",
    "prepublish": "yarn tsc",
    "pub": "yarn npm publish --access public"
  },
  "devDependencies": {
    "@types/node": "^16.10.3",
    "prettier": "^2.4.1",
    "ts-node": "^10.2.1",
    "tsc": "^2.0.3",
    "typescript": "^4.4.2"
  },
  "dependencies": {
    "@mt-inc/bot": "^1.0.10",
    "@mt-inc/indicators": "^1.0.3",
    "@mt-inc/utils": "^1.0.3"
  }
}
